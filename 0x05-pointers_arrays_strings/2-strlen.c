#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: index string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0'; index++)

		return(index);
}
